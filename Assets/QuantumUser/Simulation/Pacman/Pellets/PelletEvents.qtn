synced event PelletRespawn {
	int Configuration;
	bool PlayEffect;
	bool WipeFromLeft;
}

synced event PelletEat {
	EntityRef Entity;
	FPVector2 Tile;
	int Chain;
}

synced event PowerPelletEat {
	EntityRef Entity;
}

synced event PowerPelletEnd {
	EntityRef Entity;
}

signal OnPelletRespawn();

signal OnPelletEat();

signal OnPowerPelletStart(EntityRef entity);

signal OnPowerPelletEnd(EntityRef entity);