component GridMover {
	FP Speed;
	FP SpeedMultiplier;
	int Direction;
	bool IsLocked;
	bool IsStationary;

	FP DistanceMoved;

	FP FreezeTime;
}

signal OnGridMoverChangeTile(entity_ref entity, FPVector2 tile);